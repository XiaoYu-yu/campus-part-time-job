# Step 150 - 内测试运行状态总收口

## 本轮目标

1. 汇总 Step 142 到 Step 149 的内测运维与试用准备成果。
2. 判断当前是否还应该继续新增文档和脚本。
3. 明确下一步应等待真实内测反馈，而不是继续凭空扩需求。
4. 不改业务代码、不改 bridge、不改鉴权、不改接口、不改路由。

## Step 142 到 Step 149 已完成什么

### Step 142 - 服务器日志轮转部署与远端验证

已完成：

1. 服务器拉取并重建 compose。
2. `mysql / backend / frontend` 均验证 Docker `json-file max-size=20m max-file=5`。
3. health endpoint warm-up 后返回 `UP`。
4. remote smoke 25 项通过、0 项失败、0 项跳过。

### Step 143 - SSH 运维入口硬化清单

已完成：

1. 确认显式 identity key 登录可用。
2. 确认 password login 未关闭。
3. 新增 SSH 硬化说明。
4. 明确云安全组限制 SSH 来源 IP 的建议和回滚边界。

### Step 144 - 服务器部署后验证清单脚本化

已完成：

1. 新增 `server-post-deploy-check.ps1`。
2. 串联 remote smoke 和可选 SSH 只读检查。
3. 真实运行通过。
4. 输出脱敏报告。

### Step 145 - 内测服务器运维健康检查

已完成：

1. 新增 `server-ops-health.ps1`。
2. 只读检查磁盘、Docker、容器日志和备份目录。
3. 当前磁盘、日志、备份均无明显压力。

### Step 146 - 清理与留存策略 go / no-go

已完成：

1. 基于真实读数做 no-go 判断。
2. 当前不执行 Docker prune。
3. 当前不删除备份。
4. 当前不删除容器日志。
5. 固化清理触发条件。

### Step 147 - 内测入口状态复盘

已完成：

1. 本地与 GitHub 状态一致。
2. 服务器运行态 remote smoke 通过。
3. 当前结论：本地 / 内测型试运营可用，但不是完整产品级正式上线。

### Step 148 - 内测用户试用说明

已完成：

1. 明确参与范围。
2. 明确账号发放边界。
3. 明确 customer / parttime / admin 可测和不要测范围。
4. 明确测试前检查和异常处理顺序。

### Step 149 - 反馈记录与分级规则

已完成：

1. 反馈记录模板。
2. 阻塞 / 主要 / 次要 / 建议四类优先级。
3. 进入修复的条件。
4. 不进入当前修复的反馈类型。

## 当前总判断

当前已经达到：

```text
本地 / 内测型试运营可用
可给少量可信测试者试用
可收集真实反馈
```

当前仍不等于：

```text
完整产品级正式上线
公开注册运营
真实支付 / 退款 / 打款
正式 SLA
```

## 是否继续新增文档和脚本

结论：当前不建议继续无反馈地新增文档和脚本。

原因：

1. 部署、smoke、日志、SSH、磁盘、清理、账号发放、反馈模板都已覆盖。
2. 继续新增文档会降低信噪比。
3. 当前更需要真实用户试用反馈来驱动下一轮。
4. 没有真实问题时继续开发，容易重新进入“为了推进而推进”的状态。

## 下一步真正应该做什么

建议下一步转为：

1. owner 按内测说明发放少量账号。
2. 测试者按指南只测允许范围。
3. 所有反馈按 triage 模板记录。
4. 只有阻塞 / 主要问题进入修复。
5. 次要问题累积后成组处理。
6. 建议类反馈进入后续规划，不打断当前稳定线。

## 当前仍未完成但不阻塞内测的事项

1. HTTPS / 域名 / 证书。
2. 正式监控告警。
3. 云安全组限制 SSH `22` 来源 IP。
4. 关闭 password login。
5. 真实支付 / 退款 / 打款。
6. 正式地图导航 / 轨迹。
7. 旧外卖模块后端删除。

这些属于产品级正式上线或后续专项，不阻塞当前 owner-controlled 内测。

## 明确没有改动

本轮没有修改：

1. Java 业务代码。
2. Vue 页面。
3. `request.js`。
4. token 附着逻辑。
5. bridge 相关接口。
6. 后端鉴权。
7. 路由。
8. 数据库表结构。
9. 旧兼容模块。
10. 服务器配置。

## 验证结果

1. `git diff --check` 通过。
2. 敏感信息扫描无真实 secret / host 命中。

本轮只做总收口文档，不改业务代码，因此没有重新执行 backend compile 或 frontend build。

## 当前 bridge 结论

bridge 主线继续保持 `Phase A no-op` 冻结态。

本轮没有删除、收紧或替换：

1. `/api/campus/courier/profile`
2. `/api/campus/courier/review-status`
3. `/api/campus/courier/auth/token`

## 下一轮建议

建议暂时停止继续堆文档和脚本，进入真实内测反馈等待期。

如果必须继续推进，优先级如下：

1. owner 在云控制台限制 SSH `22` 来源 IP。
2. 服务器目录 fast-forward 到最新文档 / 脚本提交，但不重建容器。
3. 根据真实反馈修阻塞 / 主要问题。
4. HTTPS / 域名 / 证书专项。

不要继续凭空新增业务功能或页面。

## Step 151 补充

Step 151 已根据 owner 质疑补做 Android 双端稳定性复核：

1. Android 用户端 / 兼职端 public Web 构建通过。
2. 两端 Capacitor 同步通过。
3. 两端 Debug APK 在 JDK 21 下构建通过。
4. Android WebView public 聚合 smoke 默认等待参数已加固并复跑通过。
5. 当前 Android 结论修正为：双端已达到 owner-controlled 模拟器内测 smoke 可用，但仍未达到外部用户真机长期内测 / 正式试运营稳定标准。
