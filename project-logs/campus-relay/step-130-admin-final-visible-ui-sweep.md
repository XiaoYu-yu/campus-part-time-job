# Step 130 - 管理后台最终可见问题巡检与低风险修补

## 本轮目标

在 Step 128/129 已完成管理后台视觉基线和明显可用性修复后，本轮只做最后一轮低风险可见问题巡检：

1. 清理前端仍可见的旧外卖词汇残留。
2. 继续排查会导致表格右侧遮挡、拖拽条或操作列覆盖的配置。
3. 不继续扩大视觉重构范围。
4. 不改 bridge、鉴权、接口、后端、路由或 token 附着逻辑。

## 实际完成

1. 清理 `frontend/src` 范围内剩余可见旧词：
   - `Employee.vue` 中“外卖兼容模块”改为“兼容模块”。
   - `user/Home.vue` 中“旧外卖模块 / 外卖功能仍保留”改为“旧兼容模块 / 兼容功能仍保留”。
   - `user/CampusRelayOrders.vue` 中“旧外卖模块”改为“旧兼容模块”。
2. 修复通用表格组件潜在固定列问题：
   - `BaseTable.vue` 操作列移除 `fixed="right"`，避免后续复用该组件的页面重新出现右侧固定列阴影、遮罩或按钮覆盖。
3. 完成静态巡检：
   - `frontend/src` 范围内不再命中 `苍穹 / 外卖 / 旧店铺 / 骑手 / 销售额 / 总销售额 / fixed="right"`。

## 明确未改

1. 未改 bridge。
2. 未改 `request.js`。
3. 未改 token 附着逻辑。
4. 未改后端接口、数据库、鉴权或路由语义。
5. 未删除旧兼容模块。
6. 未新增页面。
7. 未做新一轮大规模 UI 重做。

## 验证结果

1. `frontend/src` 关键词巡检通过，未再发现上述旧词和 `fixed="right"`。
2. `npm run build` 通过。
3. `.\mvnw.cmd -DskipTests compile` 通过。
4. `git diff --check` 通过，仅保留 Windows 换行提示。

## 当前判断

管理后台当前已经完成一轮蓝白浅色视觉基线、按钮/表格可用性修补、旧词残留清理和明显遮挡风险收口。用户已明确认为当前样式可以接受，因此后续不建议继续默认开启样式轮次。

后续如果继续前端，只应基于用户给出的具体截图或具体页面问题做 bug 级修补，不再凭感觉继续 polish。

## 当前 bridge 结论

bridge 主线继续保持 `Phase A no-op` 冻结态：

1. 不删除 `/api/campus/courier/profile`。
2. 不删除 `/api/campus/courier/review-status`。
3. 不替换 `/api/campus/courier/auth/token`。
4. 不改 `request.js` 和 token 附着逻辑。

## 下一轮建议

Step 131 不再默认继续样式优化。建议转向：

1. 若用户继续看前端：只处理用户指出的具体页面 bug。
2. 若进入试运营准备：复核本地运行、账号、样本数据和关键 smoke。
3. 若继续功能开发：优先评估 admin 异常历史/处理前端承接是否值得做，而不是新增第五个 admin 页。
