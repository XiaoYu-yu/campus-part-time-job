# Copy to frontend/.env.android-user-public for public/internal-trial builds.
# Prefer HTTPS behind the server reverse proxy.
VITE_API_BASE_URL=https://your-domain.example.com/api
VITE_USE_MOCK=false
